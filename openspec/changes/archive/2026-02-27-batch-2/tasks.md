## 1. 后端：insights/today API

- [x] 1.1 在 `app/routes/insights.py` 添加 `GET /api/insights/today` 路由
- [x] 1.2 实现 P0：查询今天/明天有面试安排的活动记录（interview + scheduled + scheduled_at 在今明两天）
- [x] 1.3 实现 P0：为每场面试查询上轮面评摘要（last_interview_summary）
- [x] 1.4 实现 P0：查询 Offer 超 5 天无结论的活动记录（offer + payload.conclusion is null + created_at < now-5d）
- [x] 1.5 实现 P1：查询面试结束超 2 天未填面评（interview + status=scheduled + scheduled_at < now-2d）
- [x] 1.6 实现 P1：查询流程停滞超 5 天（CandidateJobLink state=IN_PROGRESS + updated_at < now-5d）
- [x] 1.7 去重逻辑：已出现在 P0 的候选人不再出现在 P1 停滞列表
- [x] 1.8 实现 P2：查询建档未分配岗位候选人（无 outcome=null 的 CandidateJobLink，deleted_at is null）
- [x] 1.9 实现 week_summary：in_progress / interviews_this_week / offers_pending / hired_this_week
- [x] 1.10 在 `main.py` 或 `app/__init__.py` 注册 insights router

## 2. 前端：今日待办页面

- [x] 2.1 在路由函数中添加 `#/today` 路由，设为默认主页（`#/` 和 `#/dashboard` 都渲染 today 页）
- [x] 2.2 导航栏新增「今日待办」为第一项，链接到 `#/today`
- [x] 2.3 实现 `renderTodayDashboard(el)` 函数 — 页面框架（标题、日期、分组结构）
- [x] 2.4 实现本周概览折叠区（默认折叠，点击展开，数据来自 week_summary）
- [x] 2.5 实现面试卡片（P0）渲染 — 收起状态：时间/姓名/轮次/岗位
- [x] 2.6 实现面试卡片展开逻辑 — 展开显示候选人简历摘要、上轮面评、操作按钮
- [x] 2.7 实现「去填面评→」跳转逻辑（跳转进行中页，URL 携带 link_id 自动展开）
- [x] 2.8 实现 Offer 等待卡片（P0）渲染 — 显示等待天数、月薪、发出日期
- [x] 2.9 实现 Offer 卡片内联表单展开（结论选择 + 备注 + 取消/保存）
- [x] 2.10 实现面评未填卡片（P1）渲染 — 显示候选人、轮次、已过天数
- [x] 2.11 实现面评未填卡片内联面评表单（面试官 → 面评 → 评分 → 结论）
- [x] 2.12 实现流程停滞卡片（P1）渲染及跳转逻辑
- [x] 2.13 实现建档未分配卡片（P2）— 折叠展开，逐条显示，每条有「分配岗位」按钮
- [x] 2.14 实现「分配岗位」按钮逻辑（复用现有 openLinkJobOverlay 或内联弹窗）
- [x] 2.15 实现操作完成后「✓ 已完成」状态 + 1.5 秒淡出消失动画
- [x] 2.16 实现空状态渲染（全部清空时显示「✓ 今天没有待办事项」）
- [x] 2.17 实现进行中页自动展开指定 link_id 的行（接收 URL 参数后自动触发展开）

## 3. 前端：进行中展开区重构

- [x] 3.1 重构 `renderExpandInner` — 提取状态判断逻辑（A/B/C/D 四个状态分支）
- [x] 3.2 实现状态A：面试未到时间 — 显示面试信息 + 「取消面试」按钮，无面评表单
- [x] 3.3 实现状态B：面试时间已过 — 面评表单自动内联展开（无需点击），字段顺序：面试官→面评→评分→结论
- [x] 3.4 实现状态C：活动完成 — 在活动卡下方独立一行显示下一步节点选项
- [x] 3.5 实现下一步节点过滤规则（根据 tail.type 过滤可用节点：面试/Offer/背调/入职）
- [x] 3.6 实现点击下一步节点后内联展开对应新建表单（面试表单 / Offer 表单 / 背调表单 / 入职表单）
- [x] 3.7 重构底部操作栏 — [备注] [退出] [淘汰] 始终可见，小字灰色
- [x] 3.8 实现「备注」内联展开（操作栏上方展开文本框 + 取消/保存）
- [x] 3.9 实现「淘汰」内联展开（操作栏上方展开淘汰原因选择 + 补充说明 + 取消/确认淘汰）
- [x] 3.10 实现同时只有一个内联表单展开（打开新的自动收起旧的）
- [x] 3.11 删除旧的「安排下一步」/「+ 备注」/「转移岗位」独立大按钮（迁移到新布局）

## 4. 样式

- [x] 4.1 今日待办页面分组标题样式（「今天」红色/橙色，「需要跟进」黄色，「待处理」灰色）
- [x] 4.2 待办卡片样式（左侧优先级色条，展开/收起箭头，卡片 hover 效果）
- [x] 4.3 「✓ 已完成」状态样式 + 淡出动画（CSS transition opacity 0 → 1.5s）
- [x] 4.4 空状态样式（居中，简洁，正向反馈感）
- [x] 4.5 底部操作栏样式（小字灰色，与主内容区视觉分层）
- [x] 4.6 下一步节点选项行样式（与历史卡片视觉区分）
